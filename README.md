# Nitter Redirector

A simple redirector that fetches a random Nitter instance from the API and redirects all requests to it, preserving the path and query parameters.

## Running

To install dependencies:
```sh
bun install
```

To run:
```sh
bun run dev
```

open http://localhost:3000
